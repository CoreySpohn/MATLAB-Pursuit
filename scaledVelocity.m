%This function takes the position and velocity data for the agents and
%robots.
function scaledVelocityResult = scaledVelocity(closestResult,currentLoc1,currentLoc2,currentLoc3,currentLoc4)
    %Calculate the direction that will be perpendicular to the agent's
    %velocity
    
    %Scale the robots' velocities to its max velocity in the direction
    %perpendicular to the agent
    
    for robotIndex = 1:size(closestResult,)
        
    end
end