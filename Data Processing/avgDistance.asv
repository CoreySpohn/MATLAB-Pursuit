function [total] = avgDistance(graphPositions)
%UNTITLED Summary of this function goes here
%   Detailed explanation goes here

loop1 = size(graphPositions,2)/2-1;
loop2 = size(graphPositions,2)/2;
total = 0;

for i =1:loop1
   
    for j = i+1:loop2
        xPos = graphPositions(i)- graphPositions(2*j-1);
        yPos = graphPositions(i+1)- graphPositions(2*j);
        dist = sqrt(xPos^2+yPos^2);
        total = total + dist;
        count = 
    end
    
end

end

